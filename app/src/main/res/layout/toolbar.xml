<?xml version="1.0" encoding="utf-8"?>
<androidx.appcompat.widget.Toolbar xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/toolbar"
    android:layout_width="match_parent"
    android:layout_height="@dimen/action_bar_height"
    android:background="?attr/colorPrimary"
    android:popupTheme="@style/ThemeOverlay.ToolbarMenu"
    android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
    app:layout_collapseMode="parallax"
    app:titleTextAppearance="@style/ToolbarTextAppearance"
    android:textColorSecondary="?attr/colorOnPrimary"
    android:subtitleTextStyle="@style/ToolbarTextAppearance">

    <RelativeLayout
        android:id="@+id/layout_main_logo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/img_title_logo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:src="@drawable/top_title_logo"
            android:tint="?attr/colorOnPrimary"/>
    </RelativeLayout>
</androidx.appcompat.widget.Toolbar>
